# /.gitignore

# ==============================================================================
# FlaskStarterKit 官方忽略规则
# 适用环境：Python 3.x + Flask + MySQL + VSCode/PyCharm
# ==============================================================================

# --- 1. Python 字节码与缓存 ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# --- 2. 环境配置与敏感信息 (绝对不能提交到 Git) ---
.env
.env.*
.flaskenv
instance/
.python-version

# --- 3. 虚拟环境 ---
venv/
env/
.venv/
ENV/
bin/
lib/
include/
share/

# --- 4. 框架运行时生成文件 ---
# 忽略所有日志文件，但在项目初始化时保留 logs/ 目录本身
logs/*
!logs/.gitkeep

# 忽略开发过程中的临时数据库文件 (如果偶尔使用 sqlite)
*.db
*.sqlite3

# --- 5. 国际化 (i18n) 编译文件 ---
# 注意：.po 文件（翻译源文件）需要提交，.mo 文件（编译后的二进制）不建议提交
# 因为它们可以通过命令在部署时重新生成
translations/**/*.mo

# --- 6. 编辑器与 IDE 配置 ---
# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# PyCharm / IntelliJ
.idea/
*.iml
*.iws

# 其他临时文件
.DS_Store
Thumbs.db
*.swp
*.swo

# --- 7. 构建与打包 ---
dist/
build/
*.egg-info/
.eggs/
.installed.cfg
*.egg
MANIFEST

# --- 8. 测试与覆盖率 ---
htmlcov/
.coverage
.coverage.*
.cache
.pytest_cache/
.tox/
.nox/

# ==============================================================================
# End of .gitignore